import java.util.logging.Logger;

import cars.*;

public class Main {

	public Main() {
	}

	final static Logger LOGGER = Logger.getLogger(Main.class.getName());

	static Car car = new Car();

	public static void main(String[] args) {

		LOGGER.info("");

		System.out.println("The colour before change:" + car.getColor());
		System.out.println("The colour after change:" + car.getColor());
		
	}

}
